spring:
  profiles:
    active: @spring.profiles.active@
  gaea:
    subscribes:
      security:
        enabled: true
      audit-log:
        enabled: true
        callback-url: http://127.0.0.1:9090/gaea/auth/log/callback

logging:
  level:
    com.alibaba.cloud.nacos.client.NacosPropertySourceBuilder: debug
